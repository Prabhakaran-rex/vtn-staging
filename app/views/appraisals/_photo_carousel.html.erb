<% if @appraisal.photos.count != 0 %>
  <div>
  <div>
    <%= link_to @appraisal.default_photo.asset.url(:huge), :class => "MagicZoom", :rel => "zoom-width: 600; zoom-height: 600; zoom-distance: 50px; show-title: false; hint: false; zoom-position: left; disable-zoom: false", :id => "zoom" do %>
      <%= image_tag @appraisal.default_photo.asset.url(:carrousel) %>
  <% end %>
  </div>
  <div id="foobar">
    <% @appraisal.photos.each_with_index do |image, i| %>
      <%= link_to image.asset.url(:huge), :rel => "zoom-id:zoom;", :rev => image.asset.url(:carrousel) do %>
      <%= image_tag image.asset.url(:thumb) %>
  <% end %>
  <% end %>
  </div>
</div>
<% end %>
