<center>
  <div class="contentBlock">
    <div class="formTop">
      <span class="title41">Forgot Password</span>
    </div>
    <%= simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post}) do |f| %>
      <%= devise_error_messages! %>
    <table>
      <tr>
        <td class="formTitle" valign="top">Email</td>
        <td class="formBlock"><%= f.input :email, label: false, input_html: {class: "input-xxlarge"} %></td>
      </tr>
      <tr>
        <td class="formTitle"></td>
        <td class="formBlock"><%= f.submit "Reset Password", :class=> "btn btn-success"%></td>
      </tr>
    </table>
  <% end %>
  </div>
</center>

