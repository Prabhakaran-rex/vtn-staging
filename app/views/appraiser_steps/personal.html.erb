
<%= render :partial => "layouts/doc_has_changed" %>
<div class="sub_box">
  <%= render :partial => "appraiser_tabs" %>
<% unless current_user.is_rejected? %>
<% if current_user.is_appraiser? %>
<div class="alert alert-info">
    <i class="icon-warning-sign"></i>  <strong>NOTICE:</strong> This information is primarily for the private use of ValueThisNow.com and will not be made public. Only the appraiser’s name, image and website will be made public.
</div>
<% end %>
  <div class="row">
    <div class="span6">
      <%= simple_form_for @user, :url => wizard_path, :html => {:class => "form-horizontal"} do |f| %>
      <%= render :partial => "validation_errors" %>

      <%= f.input :name %>
      <%= f.input :email, :disabled => true %>
      <%= f.input :website, :label => "Preferred Web Address" %>
      <%= f.simple_fields_for :address, @user.address do |a| %>
        <%= a.input :address, :required => @user.is_appraiser?  %>
        <%= a.input :address2, :label => "Address 2" %>
        <%= a.input :city, :required => @user.is_appraiser? %>
        <%= a.input :state, :label => "State/Province", :required => @user.is_appraiser? %>
        <%= a.input :country, :priority => [ "United States" ], :required => @user.is_appraiser? %>
        <%= a.input :zip, :label => "Postal Code", :required => @user.is_appraiser? %>
        <%= a.input :phone1, :label => "Phone 1", :input_html => {:class => "phone-input"}, :required => @user.is_appraiser? %>
        <%= a.input :phone2, :label => "Phone 2", :input_html => {:class => "phone-input"} %>
        <%= a.input :phone3, :label => "Phone 3", :input_html => {:class => "phone-input"} %>
        <%= a.input :fax, :input_html => {:class => "phone-input"} %>
      <% end %>
  </div>
  <div class="span3 div-gravatar">
&nbsp;
  </div>
  <div class="span3">      
  </div>
</div>
<div class="form-actions">
    <%= f.submit "Save and Continue", :class => "btn btn-success"%>
  </div>
<% end%> 
<% end %>
</div>
